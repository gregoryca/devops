---
- hosts: server17a
  become: yes
  tasks:  

  #Copy the compose file
  - name: copy Docker Compose files to remote server
    copy:
      src: ./
      dest: /home/gregory/srv/nextcloud/
    loop:
    - docker-compose.yml

  #Copy the compose file
  - name: copy Docker Compose files to remote server
    copy:
      src: /home/gregory/srv/nextcloud/data/nextcloud/nginx/site-confs/default
      dest: /home/gregory/srv/nextcloud/
    loop:
    - default

  #deploy compose file
  - name: deploy Docker Compose stack
    docker_compose:
      project_src: /home/gregory/srv/nextcloud/
      files:
      - docker-compose.yml
      recreate: always